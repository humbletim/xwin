name: Manual Trigger Placeholder
on: workflow_dispatch
jobs:
  dummy:
    runs-on: ubuntu-slim # Minimal runner for low footprint
    steps:
      - run: echo "Workflow trigger active." # Minimal step
